import { CadastroValidation } from './cadastro-validation';

describe('EmailValidation', () => {
  it('should create an instance', () => {
    expect(new CadastroValidation()).toBeTruthy();
  });
});
